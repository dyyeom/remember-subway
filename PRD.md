# 역순서 제품 요구사항 문서(PRD)

최종 갱신일: 2026-09-03  
문서 버전: 1.1  
구현 기준 브랜치: `main`  
번들 ID: `io.evolveark.remembersubway.app`

## 1. 제품 개요

`역순서`는 대한민국 도시철도 역을 인접한 역 관계로 외우는 iPhone 게임이다. 무료·무광고이며, 기본 플레이와 진행 저장은 인터넷 및 Game Center 로그인 없이 동작한다. UI는 네이티브 SwiftUI와 iOS 26 이상을 기준으로 하며 iPhone 세로 화면을 지원한다.

현재 제품은 두 가지 모드로 구성된다.

- 싱글플레이: 지역 또는 노선을 선택해 주간 도전을 플레이하고 Game Center 주간 랭킹에 기록한다.
- 멀티플레이: 근처 iPhone 2~8대가 로컬 네트워크로 같은 문제를 동시에 풀어 경쟁한다.

기존의 일반 노선 학습 모드, 구간 해금, 별점 기반 진행은 제거되었다. 과거 데이터 모델은 마이그레이션 호환성을 위해 남아 있을 수 있으나 새 UI와 게임 흐름에서는 사용하지 않는다.

## 2. 싱글플레이 요구사항

### 진입과 선택

- 하단 `싱글플레이` 탭에서 시작한다.
- 수도권, 부산, 대구, 광주, 대전 지역을 선택할 수 있다.
- 선택한 지역의 특정 노선을 고르거나 `전체 노선`을 선택할 수 있다.
- 선택한 범위의 이번 주 최고 점수와 Game Center 로그인 상태를 표시한다.
- Game Center 로그인 시 선택 범위에 맞는 주간 랭킹을 연다.

### 문제와 입력

- 이전 역과 다음 역을 보여주고 가운데 역의 이름을 입력한다.
- 역의 공식 본역명·전체 역명·검수된 별칭만 정답으로 인정한다.
- 답안은 Unicode 정규화 후 공백, 구두점, 선택적 `역` 접미사를 제거해 비교한다. 편집 거리 기반 오타 허용은 하지 않는다.
- 초성 힌트는 문제마다 한 번 사용할 수 있다.
- 문제 순서는 주간 풀과 시도별 시드를 사용해 매번 섞는다.

### 점수와 시간

- 문제당 제한 시간은 15초다.
- 모든 지역·노선에서 정답 기본 점수는 100점이다. 노선의 역 개수는 점수에 영향을 주지 않는다.
- 초성 힌트 사용 후 정답은 기본 점수의 50점에서 시작한다.
- 남은 시간이 10초 이상이면 점수 배율 100%다.
- 남은 시간이 10초 미만이면 0초까지 초당 10%씩 선형 감소한다. 예: 9초 90점, 5초 50점, 0초 0점.
- 힌트 감점을 먼저 적용한 뒤 시간 배율을 적용한다.
- 점수는 소수점 이하를 버림 처리한다.

### 오답·시간 초과·결과

- 오답 또는 시간 초과 시 하트 1개를 잃고 정답을 공개한다.
- 공개 후 약 1.5초 뒤 다음 문제로 이동한다.
- 하트가 0개가 되면 마지막 정답 공개 후 결과 화면으로 이동한다.
- 정답 피드백에는 역 이름을 포함하고 노선 고유색을 사용한다.
- 타이머, 입력창, 힌트 버튼은 키보드가 올라와도 함께 보이도록 세로 간격을 축소한다.
- 결과 화면에는 점수, 최고 기록 갱신 여부, 다시 도전, 끝내기를 표시한다.
- 점수 0점 기록은 Game Center에 제출하지 않으며, 미로그인·네트워크 실패 시 제출 대기 상태를 기기에 보관한다.

## 3. 멀티플레이 요구사항

### 로비

- 하단 `멀티플레이` 탭에서 `방 만들기` 또는 `근처 방 찾기`를 선택한다.
- 닉네임은 1~10자이며 기기에 저장한다. 중복 닉네임은 로비에서 번호로 구분한다.
- 방장은 지역, 노선, 문제 수 10개, 문제당 10초 제한을 사용한다.
- 참가 코드는 4자리이며 방장을 포함해 2~8명이다.
- 방장은 참가자를 내보낼 수 있고 2명 이상일 때 시작한다. 시작 후 신규 참가자는 차단한다.
- 로컬 네트워크 권한 거부, 잘못된 코드, 정원 초과, 콘텐츠·프로토콜 버전 불일치를 안내한다.
- 방 검색 중 멀티플레이 홈으로 돌아갈 수 있다.

### 경기

- 방장이 선택한 노선의 모든 운행 계통에서 인접 역 문제를 생성한다.
- 문제 ID는 `routePatternID + 역 인덱스`이며 동일한 이전·정답·다음 조합은 한 번만 사용한다.
- 분기선은 계통별 실제 순서를 사용하고 순환선은 양 끝 인접 관계도 포함한다.
- 풀보다 필요한 문제가 많으면 다시 섞되 같은 문제가 연속되지 않게 한다.
- 모든 기기에 3초 카운트다운, 문제·노선·타이머를 동기화한다.
- 정답 제출 또는 제한 시간 종료 시 참가자별 입력을 잠근다. 정답 공개 후 약 1.5초 뒤 다음 문제로 이동한다.
- 초성 힌트 사용 후 기본 50점, 무힌트 기본 100점, 오답마다 20점 차감, 상위 3명에게 30·20·10점 도착 보너스를 적용한다.
- 동점은 총점, 정답 수, 힌트 수, 오답 수 순으로 판정하며 모두 같으면 공동 순위다.
- 연결 해제는 15초 동안 재접속을 허용하고, 복귀하지 않으면 기권 처리한다. 방장 연결 해제 또는 복구 실패 시 경기를 취소한다.
- 결과에는 전체 순위와 본인 통계를 표시하며 1위에게 폭죽·우승 메시지를 제공한다.

### 통신 구현

- Network framework의 `NWBrowser`, `NWListener`, `NWConnection`과 Bonjour 서비스 `_rsubway._tcp`를 사용한다.
- P2P 연결을 위해 `includePeerToPeer`를 활성화하고 로컬 전용으로 광고한다.
- 방장은 정답·점수·라운드의 권한자다.
- 길이 헤더가 있는 Codable 프레임, `protocolVersion`, `contentVersion`, `matchID`, `sequenceNumber`를 사용한다.
- CryptoKit 기반 연결 암호화, 2초 하트비트, 6초 무응답 감지를 사용한다.
- 네트워크 계층은 `NearbyMatchServing` 인터페이스로 UI·게임 상태와 분리하고 `MatchCoordinator`가 상태를 조정한다.

## 4. 정보 구조와 UI

- 하단 탭은 `싱글플레이`, `멀티플레이` 두 개다.
- 기록과 설정은 각 화면의 시스템 툴바 시트에서 연다.
- 첫 실행 튜토리얼은 싱글플레이와 동일한 이전 역—현재 역—다음 역 문제로 진행한다.
- 표지판 형태의 카드, 노선 고유색 강조, 노선명·아이콘 병기를 사용한다.
- SwiftUI `TabView`, `NavigationStack`, 시트, SF Symbols, 시스템 재질을 사용한다.
- Dynamic Type, VoiceOver, 굵은 텍스트, 다크 모드, Reduce Motion, 색상 외 상태 구분, 44pt 이상 터치 영역, 촉각 피드백을 지원한다.
- 키보드 표시 시 좌우 여백은 유지하고 세로 간격만 축소한다.
- 앱 아이콘은 열차와 노선 이미지를 조합한 번들 PNG를 사용한다.

## 5. 데이터와 저장

정적 번들 `transit_data.json`은 `schemaVersion`, `contentVersion`, `challengePoolVersion`, `dataAsOf`, 출처, 지역, 운영기관, 노선, 운행 계통, 역을 포함한다. 현재 데이터에는 수도권(서울 1~9호선, 인천 1~2호선, 수인분당선, 경의중앙선, 경춘선, 경강선, 서해선, 신분당선, 공항철도 등), 부산, 대구, 광주, 대전 노선이 포함된다.

주요 공개 모델은 `Region`, `TransitOperator`, `Line`, `RoutePattern`, `Station`, `Segment`다. 노선색은 `Line.colorHex`로 관리한다.

SwiftData에는 다음을 저장한다.

- 주간 범위별 최고 점수와 Game Center 제출 대기 상태
- 튜토리얼 완료, 햅틱 설정, 멀티플레이 닉네임
- 멀티플레이 누적 경기·승리·포디움·정답률·최고 점수
- 최근 멀티플레이 경기 20개의 본인 결과
- Game Center 업적 미전송 큐

계정, 연락처, 위치, 기기 식별자, 채팅 및 원격 분석은 수집하지 않는다. 이전 일반 모드의 진행 데이터는 최초 실행 시 한 번 삭제하고 설정에 처리 여부를 기록한다. 주간 기록과 리더보드 제출 대기 데이터는 보존한다.

## 6. 접근성·오프라인·권한

- 싱글플레이는 데이터가 번들에 있어 오프라인으로 완전히 플레이 가능하다.
- Game Center 인증 실패는 일반 게임을 차단하지 않는다.
- 로컬 네트워크 권한은 멀티플레이 방 만들기 또는 근처 방 찾기 시 요청한다.
- `NSLocalNetworkUsageDescription`과 `_rsubway._tcp` Bonjour 선언을 제공한다.
- 권한 거부 시 설정 앱 안내와 싱글플레이 이동 버튼을 제공한다.

## 7. 테스트 및 완료 기준

현재 자동 테스트는 정답 정규화·초성 생성, 노선·분기·순환 문제 생성, 주간 시드 셔플, 100점 고정 기본 점수, 힌트·시간 배율, 시간 초과·하트·정답 공개, 멀티플레이 점수·동점 순위·프로토콜, 저장소 마이그레이션을 검증한다.

출시 전 추가 검증 항목:

- 실제 iPhone 2~8대의 Wi-Fi 근거리 방 생성·입장·완주
- 로컬 네트워크 권한 허용·거부, 연결 해제·재접속, 백그라운드 전환
- 키보드가 열린 소형 iPhone, 최대 Dynamic Type, VoiceOver, 다크 모드, Reduce Motion
- 모든 대상 노선의 역 ID 참조·인접 순서·출처·기준일 검증
- Game Center 미로그인, 취소, 네트워크 실패와 제출 재시도

## 8. 변경 관리

앞으로 기능·규칙·데이터·화면·권한·저장 모델·테스트 기준을 변경할 때마다 이 문서의 관련 절을 같은 작업 단위에서 갱신한다. 변경 시 다음을 기록한다.

1. 최종 갱신일과 문서 버전
2. 변경된 요구사항과 사용자에게 보이는 동작
3. 데이터·저장·통신·권한 영향
4. 추가·수정한 테스트와 검증 결과
5. 필요한 마이그레이션 또는 호환성 주의사항

### 변경 이력

- 1.1 (2026-09-03): 현재 구현을 기준으로 문서화. 싱글플레이 지역·노선 선택, 노선 무관 100점 기준, 15초 시간 배율과 시간 초과 처리, 멀티플레이·String Catalog·데이터·저장·테스트 현황을 반영했다.

